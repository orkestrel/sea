import type { PlatformConfig } from "./types.js";
import { join, resolve } from "node:path";

// === Build Paths

/** Project root directory */
export const ROOT_DIR = resolve(".");

/** Build output directory */
export const DIST_DIR = join(ROOT_DIR, "dist");

/** Client build output directory */
export const DIST_CLIENT_DIR = join(DIST_DIR, "client");

/** Server build output directory */
export const DIST_SERVER_DIR = join(DIST_DIR, "server");

/** SEA configuration file path */
export const SEA_CONFIG_PATH = join(ROOT_DIR, "configs", "sea-config.json");

/** SEA blob output path */
export const SEA_BLOB_PATH = join(DIST_DIR, "sea-prep.blob");

/** SEA sentinel fuse identifier */
export const SEA_SENTINEL_FUSE = "NODE_SEA_FUSE_fce680ab2cc467b6e072b8b5df1996b2";

/** SEA blob resource name */
export const SEA_BLOB_RESOURCE = "NODE_SEA_BLOB";

/** Default Brotli compression quality */
export const DEFAULT_COMPRESSION_QUALITY = 11;

// === PE Subsystem (Windows)

/** PE subsystem value for console applications (shows terminal window) */
export const PE_SUBSYSTEM_CONSOLE = 3;

/** PE subsystem value for GUI applications (no terminal window) */
export const PE_SUBSYSTEM_WINDOWS = 2;

// === Platform

/** Platform-specific build configurations indexed by OS */
export const PLATFORMS: Record<NodeJS.Platform, PlatformConfig> = {
  win32: {
    executable: "tsea.exe",
    removeSignature: true,
    resign: false,
    postjectArgs: Object.freeze([]),
  },
  darwin: {
    executable: "tsea",
    removeSignature: true,
    resign: true,
    postjectArgs: Object.freeze(["--macho-segment-name", "NODE_SEA"]),
  },
  linux: {
    executable: "tsea",
    removeSignature: false,
    resign: false,
    postjectArgs: Object.freeze([]),
  },
  aix: {
    executable: "tsea",
    removeSignature: false,
    resign: false,
    postjectArgs: Object.freeze([]),
  },
  android: {
    executable: "tsea",
    removeSignature: false,
    resign: false,
    postjectArgs: Object.freeze([]),
  },
  cygwin: {
    executable: "tsea.exe",
    removeSignature: false,
    resign: false,
    postjectArgs: Object.freeze([]),
  },
  freebsd: {
    executable: "tsea",
    removeSignature: false,
    resign: false,
    postjectArgs: Object.freeze([]),
  },
  haiku: {
    executable: "tsea",
    removeSignature: false,
    resign: false,
    postjectArgs: Object.freeze([]),
  },
  netbsd: {
    executable: "tsea",
    removeSignature: false,
    resign: false,
    postjectArgs: Object.freeze([]),
  },
  openbsd: {
    executable: "tsea",
    removeSignature: false,
    resign: false,
    postjectArgs: Object.freeze([]),
  },
  sunos: {
    executable: "tsea",
    removeSignature: false,
    resign: false,
    postjectArgs: Object.freeze([]),
  },
};
